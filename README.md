This module is used to install and configure Nagios on a node
of the "Cloud dell'area Padovana".

This module:
- creates the nagios account and configure the ssh public key
- install the needed nagios plugin packages
